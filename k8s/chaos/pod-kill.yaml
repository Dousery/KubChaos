apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: demo-http-pod-kill
  namespace: chaos-mesh
spec:
  action: pod-kill
  mode: fixed
  value: "1"
  selector:
    namespaces:
      - test-app
    labelSelectors:
      app: demo-api
  scheduler:
    cron: "@every 1m"

