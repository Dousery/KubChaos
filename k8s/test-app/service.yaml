apiVersion: v1
kind: Service
metadata:
  name: demo-http
  namespace: test-app
spec:
  type: NodePort
  selector:
    app: demo-http
  ports:
    - name: http
      port: 80
      targetPort: 5678
      nodePort: 30080

